# @rneui/base@5 declares @testing-library/* as runtime peer dependencies, and
# those pull react-test-renderer@19.2, which conflicts with the react@19.1 that
# Expo SDK 54 pins. The conflict is in RNE's metadata, not in what actually
# runs, so install with the legacy resolver instead of failing.
legacy-peer-deps=true
